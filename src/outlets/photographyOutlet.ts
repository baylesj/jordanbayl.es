import { Outlet } from '@dojo/framework/routing/Outlet';

import Photography from './../widgets/photography';

export const PhotographyOutlet = Outlet(Photography, 'photography');

export default PhotographyOutlet;