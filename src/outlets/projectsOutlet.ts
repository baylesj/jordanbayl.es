import { Outlet } from '@dojo/framework/routing/Outlet';

import Projects from './../widgets/projects';

export const ProjectsOutlet = Outlet(Projects, 'projects');

export default ProjectsOutlet;